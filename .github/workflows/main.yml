name: node template project

on:
  push:
    branches:
      - main

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello world"
      - name: Checkout
        uses: actions/checkout@v4
      - name: list files
        run: ls -a
